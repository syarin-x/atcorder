#include <bits/stdc++.h>

using namespace std;

#define rep(i,n)      for(long long(i)=0;(i)<(n);(i)++)
#define kiriage(a,b)  ((a)+(b)-1)/(b)

int main(){

    string s;
    cin >> s;

    long ans = stol(s);

    if(errno == )

    return 0;
}
